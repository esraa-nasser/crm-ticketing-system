<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <OverrideHtmlAssetPlaceholders>true</OverrideHtmlAssetPlaceholders>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" PrivateAssets="all" />
    <PackageReference Include="Microsoft.Extensions.Http" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\CrmTicketing.Shared\CrmTicketing.Shared.csproj" />
  </ItemGroup>

  <!-- TicketsApiClient.PageSize is the single declaration of the page size the client
       requests; the tests assert against it rather than repeating the literal.
       Matches the grant already carried by Api and Infrastructure. -->
  <ItemGroup>
    <InternalsVisibleTo Include="CrmTicketing.Client.Tests" />
  </ItemGroup>

</Project>
